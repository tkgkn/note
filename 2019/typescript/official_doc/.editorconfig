# 配合EditorConfig for VS Code插件使用

# 读取配置文件到这里就是最终的
root = true

# 所有文件
[*]
charset = utf-8
end_of_line = lf
indent_size = 2
indent_style = space
insert_final_newline = true
trim_trailing_whitespace = true
